# TODO

## The task

Implement an authentication in `apps/api` project. 

## Agent documentation directory

For all instructions in this task, `<agent-docs-directory>` means `.agent-docs`, relative to the repository root.

## Task name

All generated `.md` files should be in format `<task-name>-file-name.md`. The `<task-name>` should be provided in task description. If it wasn't you should ask for it when its needed.

## Specs

But before, read and follow the list of specs in `<agent-docs-directory>/specs`  first:
- `basic.spec.md`
- `investigation-to-implementation-plan-report.spec.md`
